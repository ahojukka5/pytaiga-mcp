# Docker environment variables template
# Copy this file to .env and fill in your values

# Taiga API URL (default: https://api.taiga.io)
TAIGA_API_URL=https://api.taiga.io

# Authentication (RECOMMENDED: Use application token)
# Get from: Taiga Web UI → User Settings → Applications → Create new application
TAIGA_AUTH_TOKEN=your_application_token_here
TAIGA_TOKEN_TYPE=Application

# Logging
LOG_LEVEL=INFO
# LOG_FILE=/app/logs/taiga-mcp.log

# Server Configuration (usually don't need to change these)
# FASTMCP_HOST=0.0.0.0
# FASTMCP_PORT=8000
