# Taiga MCP Bridge - Environment Configuration Example
# Copy this file to .env and fill in your values
# NEVER commit .env to version control (add to .gitignore)

# ===================================================================
# RECOMMENDED: Application Token Authentication (Simplest & Most Secure)
# ===================================================================
# Get your Application Token from Taiga:
# 1. Login to Taiga web interface
# 2. Go to: Settings → Applications  
# 3. Create new application
# 4. Copy the token here

TAIGA_HOST=https://api.taiga.io
TAIGA_AUTH_TOKEN=your_application_token_here
TAIGA_TOKEN_TYPE=Application

# ===================================================================
# Optional: MCP Server Configuration
# ===================================================================

# Transport mode: stdio (terminal) or sse (web-based)
TAIGA_TRANSPORT=stdio

# API base URL (if using self-hosted Taiga)
TAIGA_API_URL=https://api.taiga.io

# Session timeout in seconds (default: 8 hours)
SESSION_EXPIRY=28800

# API request timeout in seconds
REQUEST_TIMEOUT=30

# HTTP connection pool settings
MAX_CONNECTIONS=10
MAX_KEEPALIVE_CONNECTIONS=5

# Rate limiting (requests per minute)
RATE_LIMIT_REQUESTS=100

# Logging
LOG_LEVEL=INFO
LOG_FILE=taiga_mcp.log

# ===================================================================
# NOT RECOMMENDED: Username/Password (Less Secure)
# ===================================================================
# Only use for initial setup to get a token, then DELETE these!
# 
# TAIGA_USERNAME=your_username
# TAIGA_PASSWORD=your_password
#
# After first login:
# 1. Use save_session_token tool to cache the token
# 2. DELETE TAIGA_PASSWORD from this file
# 3. Use login_from_cache for future authentications
